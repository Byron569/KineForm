# 本次项目修改约束（项目基本约束）

> 适用范围：KineForm 后续所有开发（Python 分析管线 + Electron 标注客户端 + 本地后端）。
> 制定日期：2026-08-26。修改任何代码前先读本文件。

## 规则 1：算法优先开源复用

实现任何运动科学/信号处理算法前，**必须先查找开源成熟实现**：

1. 优先级：权威库（scipy / numpy / mmpose 生态）→ 高星开源项目（运动科学、步态分析）→ 论文官方代码
2. 找到可靠实现直接调用/移植，**禁止凭模糊记忆自造算法**
3. 查找后仍不确定 → **优先来问我**，不要自行猜测实现

## 规则 2：最小修改原则

1. 只做直接需要的改动，不顺手重构、不批量改风格、不动无关代码
2. 新功能优先在**现有扩展点**上添加（如 events/detector.py 的 `register_event_detector` 注册架构），不新建平行体系
3. 修 bug 只修根因，不引入防御性冗余代码
4. 每次改动保持可独立验证（跑通测试）后才进入下一项

## 规则 3：模糊即问

对以下情况必须先问再动手：

- 论文/报告中的定义存在多种解释（如事件判据、角度约定）
- 不清楚用户期望的输出格式或展示方式
- 开源方案有多个候选，取舍不明

## 规则 4：积极进行子代理派发但是必须先传约束

积极派发任何子代理（探索/实现/调研）时，任务描述中**必须先附带本文件的三条基本规则**（开源优先、最小修改、模糊即问），再写具体任务。子代理产出若违反约束（如自造算法、大范围重构），主会话不予采纳并要求重做。

## 附：当前进行中的工作

- P0 已完成：步态事件检测（Zeni 方法，移植自 myogait/MIT）+ 时空参数（触地/腾空/周期/步频/支撑比）→ events.json gait_summary
- P1 已完成：节段角（大腿/小腿，segment_angles.py）+ 每步事件条件化特征（gait_step_features：TD/midstance/TO 快照、膝塌陷、支撑 ROM、摆动最小膝角、最大抬腿、脚-骨盆距离、触地前脚相对速度、骨盆垂直振幅）→ events.json gait_summary.steps；motion.json 每帧 34 键
- P2 已完成（2026-08-26，93 项测试通过）：
  - 肩角采用 Hanley 2020 解剖约定（0°=站立下垂，前摆正/后摆负）——唯一有精英参考数值的约定；已实现 segment_angles.shoulder_angle_series，肘角（180°=伸直）天然与 Hanley 兼容
  - 尺度归一化：scale.py（body_height_px / leg_length_px / m_per_px / speed_px_s / speed_mps）；steps 增加 stride_length_px/norm/m、foot_pelvis_dist_norm、pelvis_vertical_range_norm；设置项 athlete_height_m + CLI --height
  - 精英参考库 references.py：sprint_max_v / marathon_m / marathon_f（自动 z-score 对比，band: common/deviated/outlier）+ hj_takeoff_success / marathon_800m_f（已收录不自动比）；每条带 source/sample/angle_definition/comparability；无 SD 文献值只出参考值；马拉松踝角标记不可比（站立 110° vs 本项目 ~90°）
  - 顺手修复存量 bug：halpe26.body_height_px 在图像 y 向下坐标系恒返回 None → 后处理尖峰跳点检测（规则 A）此前从未生效，已修复
- 疲劳趋势已完成：events/fatigue.py（前/后半程 split-half 对比，输出 halves/delta/delta_pct，步数<6 为 null）→ gait_summary.fatigue_trend
- 起跳步识别已完成：events/takeoff.py（末步 + 触地时间 >1.4×中位数判据，保守宁漏报不误报）→ gait_summary.takeoff_steps；识别到起跳步时自动追加 hj_takeoff_success 参考对比
- Wave3 GUI 已完成：ui/dialogs.py 新增 GaitDetailDialog（左右时空参数/每步特征/参考对比 z-band/疲劳趋势展示），main_window.py 结果区新增「步态详情…」入口
- 契约文档已同步：client/API_CONTRACT.md §5 扩充为 gait_summary 完整契约（生产者/消费者清单、字段表、z-score band 语义、angle_definition 与 skipped_metrics、additive/breaking 兼容规则）
- 数据集提取与模型训练：按用户指示跳过，后续进行
- 约束提醒：短跑支撑期 85-110ms，30fps 仅约 3 帧/支撑期，事件检测需带帧率适配与降级提示

