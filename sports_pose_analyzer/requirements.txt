# 固定版本依赖清单（Python 3.11 / Windows / CUDA 12.1）
# 安装方法见 README.md「安装」一节（torch/mmcv 需专用源，不能直接 pip install -r）

# --- PyTorch（CUDA 12.1 官方源）---
# pip install torch==2.1.2 torchvision==0.16.2 --index-url https://download.pytorch.org/whl/cu121
torch==2.1.2+cu121
torchvision==0.16.2+cu121

# --- mmcv 预编译轮子（OpenMMLab 官方，cu121/torch2.1.0）---
# pip install https://download.openmmlab.com/mmcv/dist/cu121/torch2.1.0/mmcv-2.1.0-cp311-cp311-win_amd64.whl
mmcv==2.1.0

# --- OpenMMLab 推理栈 ---
mmdet==3.3.0
mmpose==1.3.2
mmengine==0.10.7
xtcocotools==1.14.3

# --- 数值/图像 ---
numpy==1.26.4          # 必须用 1.x：torch 2.1.2 与 numpy 2.x 二进制不兼容
opencv-python==4.11.0.86
scipy==1.17.1
pillow==12.3.0

# --- 视频转码 ---
imageio-ffmpeg==0.6.0  # 无系统 ffmpeg 时提供内置 ffmpeg

# --- GUI ---
PySide6==6.11.2

# --- 工具（mmpose 传递依赖 chumpy 构建需要）---
setuptools<81          # mmengine 依赖 pkg_resources，>=81 已移除

# --- 测试 ---
pytest==9.1.1

# 传递依赖（供参考，上面各项会自动带入）
# matplotlib==3.11.1
